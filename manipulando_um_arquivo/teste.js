// Isso é um comentario de uma linha

/* 
Isso é 
um  de 
várias linhas
*/

var desconto = 0.02; // Valores com var pode ser alterado
const PRECO = 2; // Valores com const não podem ser alteradas
var total = PRECO - desconto;
